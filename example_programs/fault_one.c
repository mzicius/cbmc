int main(void){
    //int y = 3;
    //int x = 4;

    /*
    //working ones:
    assert(x==4);
    assert(x>1);
    assert(x>y);
    assert(y<x);
    assert(y>x);
    assert(x+1>y);
    //interesting one
    assert(x-1==y);
    assert(5>=x);
    assert(x>=y+1);
    assert(-100<=-99);
    assert(-x<y);
    assert(-x+10>y);
    assert(x!=y);
    assert(x+1+1+1>4);
    assert(x+x+x==13);
    assert(x+x+x==12);
    assert(x-x-x-x==-10);
    assert(x-x==0);
    assert(x-x==1);
    //x-5 is -1 so all of the following two asserts are nonsense
    assert(x-5==-2);
    assert(x-5==-3);
    assert(x-5 == 2);
    assert(x-5 == 4);
    */
   // right hand side 
    //assert(-2==1);
    assert(1==0);
    //assert(5>3);
    //assert(0==1);
    //int i = -1;

    //./goto-analyzer ../../example_programs/fault_one.c  --verify --tvpireturn 0;
}